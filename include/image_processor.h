#include <string>

void ResizeImage(const std::string &filepath,
                 const std::string &_outdir,
                 int _size,
                 int _quality,
                 int _width,
                 int _height);